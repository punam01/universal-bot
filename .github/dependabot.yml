version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5

  # Python + npm updaters activate automatically when those lockfiles land in Phase 1.
  # Uncomment then:
  #
  # - package-ecosystem: "pip"
  #   directory: "/api"
  #   schedule: { interval: "weekly" }
  #
  # - package-ecosystem: "npm"
  #   directory: "/web"
  #   schedule: { interval: "weekly" }
